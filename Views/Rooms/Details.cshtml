@model DormMS.Web.Models.Room

@{
    ViewData["Title"] = "Room Details";
}

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<div class="container mt-5">
    
    <div class="mb-4">
        <a asp-action="Index" class="btn-back-premium text-decoration-none shadow-sm">
            <i class="bi bi-arrow-left"></i> Back to Inventory
        </a>
    </div>

    <div class="form-card-premium shadow-sm">
        
        <div class="d-flex justify-content-between align-items-center mb-5 border-bottom pb-4">
            <div>
                <h1 style="color: #312e81; font-weight: 900; font-size: 2.8rem; letter-spacing: -2px; margin: 0;">
                    Room <span style="color: #6366f1;">@Model.roomNumber</span>
                </h1>
                <p class="text-muted fw-bold mt-1 mb-0">Unit Specification & Operational Status</p>
            </div>

            <span class="badge @(Model.status == "Available" ? "badge-available" :
                                                   Model.status == "Occupied" ? "badge-occupied" :
                                                   Model.status == "Maintenance" ? "badge-maintenance" : "badge-archived")"
                  style="font-size: 0.8rem; padding: 10px 22px; border-radius:100px; font-weight:800; text-transform:uppercase; letter-spacing:1px;">
                @Model.status
            </span>
        </div>

        <div class="row g-4">
            
            <div class="col-md-6">
                <div class="input-wrapper-premium">
                    <label class="label-premium">Building Location</label>
                    <div class="dorm-input-premium d-flex align-items-center">
                        <i class="bi bi-building me-2 text-primary"></i>
                        @Model.Building?.buildingName
                    </div>
                </div>

                <div class="input-wrapper-premium">
                    <label class="label-premium">Room Classification</label>
                    <div class="dorm-input-premium d-flex align-items-center justify-content-between">
                        <div class="d-flex align-items-center">
                            <i class="bi bi-tag me-2 text-primary"></i>
                            @Model.RoomType?.typeName
                        </div>
                        @if(Model.RoomType?.hasBathroom == true) {
                            <span class="badge bg-primary-subtle text-primary border border-primary fw-bold" style="font-size: 10px;">
                                <i class="bi bi-water me-1"></i> PRIVATE BATH
                            </span>
                        } else {
                            <span class="badge bg-light text-muted border fw-bold" style="font-size: 10px;">
                                <i class="bi bi-slash-circle me-1"></i> SHARED BATH
                            </span>
                        }
                    </div>
                </div>
            </div>

            <div class="col-md-6">
                <div class="occupancy-glass-card h-100 d-flex flex-column justify-content-center">
                    <label class="label-premium">Occupancy Status</label>
                    <div class="d-flex justify-content-between align-items-end mb-2">
                        <span class="stat-value-premium">@(Model.currentOccupancy ?? 0)</span>
                        <span class="text-muted fw-bold">/ @Model.capacity Residents</span>
                    </div>
                    <div class="progress" style="height: 10px; border-radius: 10px; background: #e2e8f0;">
                        @{
                            var percent = Model.capacity > 0 ? ((Model.currentOccupancy ?? 0) * 100) / Model.capacity : 0;
                        }
                        <div class="progress-premium-bar" style="width: @percent%"></div>
                    </div>
                </div>
            </div>
            
            <div class="col-lg-7">
                <div class="section-title-premium mt-4">
                    <i class="bi bi-people"></i> Current Residents
                </div>
                <div class="table-responsive bg-white rounded-4 border p-2">
                    <table class="table table-hover align-middle m-0">
                        <thead class="small text-muted">
                            <tr><th>RESIDENT</th><th>STATUS</th><th class="text-end">ACTIONS</th></tr>
                        </thead>
                        <tbody>
                            @if (Model.Allocations != null && Model.Allocations.Any())
                            {
                                foreach (var acc in Model.Allocations)
                                {
                                    <tr>
                                        <td>
                                            <div class="d-flex align-items-center">
                                                <div class="avatar-circle-sm bg-primary-subtle text-primary fw-bold me-2">
                                                    @(acc.Student?.User?.firstName?[0])
                                                </div>
                                                <div>
                                                    <div class="fw-bold text-dark small">@acc.Student?.User?.firstName @acc.Student?.User?.lastName</div>
                                                    <div class="text-muted" style="font-size: 10px;">@acc.Student?.studentId</div>
                                                </div>
                                            </div>
                                        </td>
                                        <td><span class="badge @(acc.status == "Checked-In" ? "bg-success-subtle text-success" : "bg-warning-subtle text-warning") rounded-pill px-2" style="font-size: 9px;">@acc.status</span></td>
                                        <td class="text-end">
                                            <a asp-controller="Students" asp-action="Details" asp-route-id="@acc.studentId" class="btn btn-sm btn-light rounded-circle shadow-none"><i class="bi bi-eye"></i></a>
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr><td colspan="3" class="text-center py-4 text-muted small">No active residents recorded.</td></tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>

            <div class="col-lg-5">
                <div class="section-title-premium mt-4 text-danger">
                    <i class="bi bi-wrench-adjustable"></i> Facility Backlog
                </div>
                <div class="maintenance-summary-list bg-white rounded-4 border p-3">
                    @if (Model.MaintenanceRequests != null && Model.MaintenanceRequests.Any())
                    {
                        foreach (var req in Model.MaintenanceRequests.Take(5))
                        {
                            <div class="d-flex align-items-center justify-content-between p-2 mb-2 rounded-3 @(req.status == "Pending" ? "bg-danger-subtle" : "bg-light")">
                                <div class="overflow-hidden">
                                    <div class="fw-bold text-dark small text-truncate">@req.issueCategory</div>
                                    <div class="text-muted" style="font-size: 10px;">@req.requestDate?.ToString("dd MMM yyyy")</div>
                                </div>
                                <span class="badge @(req.priority == "High" ? "bg-danger" : "bg-warning text-dark") rounded-pill" style="font-size: 8px;">@req.priority</span>
                            </div>
                        }
                    }
                    else
                    {
                        <div class="text-center py-5">
                            <i class="bi bi-shield-check display-6 text-success opacity-50 mb-2 d-block"></i>
                            <p class="text-muted small m-0">No open maintenance issues.</p>
                        </div>
                    }
                </div>
            </div>

            @if (!string.IsNullOrEmpty(Model.notes))
            {
                <div class="col-12 mt-4">
                    <div class="p-4 rounded-4" style="background: #f8fafc; border: 1.5px dashed #cbd5e1;">
                        <label class="label-premium text-primary mb-2">
                            <i class="bi bi-journal-text me-1"></i> Internal Admin Notes
                        </label>
                        <p class="text-dark fw-medium mb-0" style="line-height: 1.6;">@Model.notes</p>
                    </div>
                </div>
            }
        </div>

        <div class="d-flex align-items-stretch gap-3 mt-5">
            <a asp-action="Edit" asp-route-id="@Model.id" class="btn-premium flex-grow-1 d-flex align-items-center justify-content-center text-center text-decoration-none shadow-sm">
                <i class="bi bi-pencil-square me-2"></i> Edit Room
            </a>

            @if (Model.status == "Archived")
            {
                <form id="restoreForm" asp-action="Restore" asp-route-id="@Model.id" method="post" class="flex-grow-1 m-0">
                    @Html.AntiForgeryToken()
                    <button type="button" class="btn-premium w-100 h-100 d-flex align-items-center justify-content-center shadow-sm"
                            style="background: linear-gradient(135deg, #10b981 0%, #059669 100%) !important;" onclick="confirmRestore()">
                        <i class="bi bi-arrow-counterclockwise me-2"></i> Restore Room
                    </button>
                </form>
            }
            else
            {
                <form id="archiveForm" asp-action="Archive" asp-route-id="@Model.id" method="post" class="flex-grow-1 m-0">
                    @Html.AntiForgeryToken()
                    <button type="button" class="btn-discard-premium w-100 h-100 d-flex align-items-center justify-content-center shadow-sm" onclick="confirmArchive()">
                        <i class="bi bi-archive me-2"></i> Archive Room
                    </button>
                </form>
            }
        </div>
    </div>
</div>

<script>
    function confirmArchive() {
        Swal.fire({
            title: 'Archive Room?',
            text: "This unit will be hidden from active inventory.",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#f43f5e',
            cancelButtonColor: '#64748b',
            confirmButtonText: 'Yes, archive it!',
            background: '#ffffff',
            customClass: { popup: 'rounded-4 shadow-lg' }
        }).then((result) => { if (result.isConfirmed) { document.getElementById('archiveForm').submit(); } })
    }

    function confirmRestore() {
        Swal.fire({
            title: 'Restore Room?',
            text: "This unit will be moved back to active status.",
            icon: 'info',
            showCancelButton: true,
            confirmButtonColor: '#10b981',
            cancelButtonColor: '#64748b',
            confirmButtonText: 'Yes, restore it!',
            background: '#ffffff',
            customClass: { popup: 'rounded-4 shadow-lg' }
        }).then((result) => { if (result.isConfirmed) { document.getElementById('restoreForm').submit(); } })
    }
</script>


